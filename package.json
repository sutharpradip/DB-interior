{
  "name": "json-server-railway",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "npx json-server --watch db.json --port 3000",
    "postinstall": "chmod +x ./node_modules/.bin/json-server"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
